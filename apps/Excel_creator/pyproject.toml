[project]
name = "excel-creator"
version = "0.1.0"
description = "HySPRINT Excel creator app"
requires-python = ">=3.10"
dependencies = [
    "hysprint-utils",
    "ipython>=8.12",
    "ipywidgets>=8.1",
    "openpyxl>=3.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["."]