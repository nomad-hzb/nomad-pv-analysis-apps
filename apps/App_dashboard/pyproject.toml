[project]
name = "app-dashboard"
version = "0.5.1"
description = "HySPRINT App dashboard app"
requires-python = ">=3.10"
dependencies = [
    "hysprint-utils",
    "ipython>=8.12",
    "ipywidgets>=8.1",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
